let TaggedUser;_a10‍.x([["tag",()=>tag]]);_a10‍.w("../../models/taggedUser",[["default",["TaggedUser"],function(v){TaggedUser=v}]]);


// 태그 하기 
       const tag = async (ctx) => {
    const tag = new TaggedUser({
        taggedUser:ctx.state.user,
        post:{
            _id: ctx.state.post._id,
            contents: ctx.state.post.contents,
            username: ctx.state.post.user.username
        },
    });
    _a10‍.g.console.log(tag);

    try{
        await tag.save();
        ctx.body = tag;
    }catch(e){
        ctx.throw(500,e);
    }
}