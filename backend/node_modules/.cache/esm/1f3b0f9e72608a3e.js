let mongoose,Schema;_726‍.x([["default",()=>_726‍.o]]);_726‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["Schema",["Schema"],function(v){Schema=v}]]);

const PostSchema = new Schema({
    tags:[String], // 태그
    publishedDate:{ // 게시일
        type:Date,
        default:Date.now,
    },
    user:{ // 글쓴이
        _id:mongoose.Types.ObjectId,
        username: String,
    },
    contents : { // 내용
        type: String,
        required: true,
    },
    like:{ // 좋아요 수 
        type:Number,
        default: 0,
    },
    img:{ // 아마도 파일경로
        type:String,
    //    required: true,
    },
    comments:{
        type:Number,
        default: 0,
    },
});

const Post = mongoose.model('Post',PostSchema);
_726‍.d(Post);