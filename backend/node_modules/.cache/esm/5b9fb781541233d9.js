let UserFollow;_541‍.x([["following",()=>following],["remove",()=>remove],["list",()=>list]]);_541‍.w("../../models/userFollow",[["default",["UserFollow"],function(v){UserFollow=v}]]);

// 팔로우
       const following = async (ctx) => {
    try{
        const {recieverid, senderid} = ctx.request.body;

        const following = new UserFollow({
            recieverid,
            senderid
        });

        await following.save();
        
    }catch(e){
        throw(500,e);
    }
}

// 팔로우취소
       const remove = async (ctx) => {
    const { id } = ctx.params;
    try{
        await UserFollow.findByIdAndDelete(id).exec();
        ctx.status = 204;
    }catch(e){
        ctx.throw(500,e);
    }
}

//팔로우 리스트
// GET /api/posts?id=
       const list = async (ctx) => {
    const { id } = ctx.query;
}